**(5d)**

> PGOS已提供相关接口，修改Pgos的客户端封装类，提供一个修改备注的接口即可。


### 插件提供功能：

plugins/PgosSdk/.../PgosClientDataModels.h
```cpp
struct PGOSSDKCPP_API FPgosClientPlayerFriendInfo
{
    GENERATED_USTRUCT_BODY()

    /** Basic player information such as display name and avatar. */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "PGOS|Client|Models")
    FPgosPlayerInfo player_info;

    /** The player's current status information which is set by game, such as in party , in lobby, in battle. */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "PGOS|Client|Models")
    FPgosClientPlayerPresence player_presence;

    /** remark to the player, for example, "Tommy, a humorous man, met in the battle." */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "PGOS|Client|Models")
    FString remark;
};

struct PGOSSDKCPP_API FPgosClientSetFriendRemarkParams
{
// ...
    FString player_id;
    FString remark;
};

```

plugins/PgosSdk/.../PgosClientFriendAPI.h
```cpp
  
namespace Pgos {
    namespace Client {

        class FPgosFriendAPI : public TSharedFromThis<...>
        {            
	        void GetFriendList(FPgosClientOnGetFriendList ResultDelegate) const;

            /**
             * Set remark to your friend, for example, "Tommy, a humorous man, met in the battle.",  Max remark length: 32 characters.
             *
             * @param Params Request struct for set friend remark
             * @param ResultDelegate The result delegate after the API execution ends, and it will be called in the GAME THREAD.
             */

            void SetFriendRemark(
                const FPgosClientSetFriendRemarkParams& Params,
                FPgosOnApiResult ResultDelegate) const;
                
            // ...
        };
```





### 客户端Pgos封装

```cpp

// 接口类， 提供了虚函数占位
class UFriendsManagerBase : public ...
{
	virtual bool GetFriendList();

	// 尚未提供设置备注接口

};


// 实现类，使用PgosSdk的GetClientFriendAPI()提供的接口来向Pgos请求数据
// ...
class UFriendsManagerPGOS : public UFriendsManagerBase
{
	// ...
	bool GetFriendList() override
	{
		PgosClientFriendPtr FriendPtr = IPgosSDKCpp::Get().GetClientFriendAPI();
		
		FriendPtr->GetFriendList([WeakThis](const FPgosResult& Ret, 
				const Pgos::Client::FClientFriendListInfo* Data) {
                    WeakThis->GetFriendListCallBack(Ret, Data);
            });
	}
	
	void GetFriendListCallBack(
		const FPgosResult& Ret, 
		const Pgos::Client::FClientFriendListInfo* Data )
	{
		// 简单处理后调用代理广播，通知UI模块
	}
	
	// 尚未实现设置备注接口
		
	//...
};
  

bool UFriendsManagerPGOS::RegistEvent()
{
    if (!bRegistStatus)
    {
        PgosClientFriendPtr FriendPtr = IPgosSDKCpp::Get().GetClientFriendAPI();
        if(FriendPtr.IsValid())
        {
            FriendPtr->OnAddFriendRequest().AddUObject(this, &UFriendsManagerPGOS::OnAddFriendRequest);

            FriendPtr->OnAddFriendResponse().AddUObject(this, &UFriendsManagerPGOS::OnAddFriendResponse);

            FriendPtr->OnFriendRemoved().AddUObject(this, &UFriendsManagerPGOS::OnFriendRemoved);

            FriendPtr->OnBadgeNumOfFriendReq().AddUObject(this, &UFriendsManagerPGOS::OnBadgeNumOfFriendReq);

            FriendPtr->OnFriendPresenceChanged().AddUObject(this, &UFriendsManagerPGOS::OnFriendPresenceChanged);

            // zhangbj: 添加好友备注功能接口
			// ...
        }

		// ...
    }

    return bRegistStatus;
}
```


PlayerID：

![[{F8070B61-FD6C-4665-B0DA-6A1219122069}.png]]

## 实现：
	接口：FriendsManagerBase::SetFriendRemarker
	结果代理：EventSetFriendRemarkerResult
	实现：UFriendsManagerPGOS类中
	GM命令：UGOLCheatManager::GM_SetFriendRemarker
	Lua: FriendView.lua/M:EventSetFriendRemarkerResult(...)
	错误码: 见lua中注释


